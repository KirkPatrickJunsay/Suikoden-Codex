namespace SuikodenCodex;

/// <summary>Compile-time feature toggles. Flip to re-enable hidden features in a future release.</summary>
public static class FeatureFlags
{
    /// <summary>Card Duel minigame — hidden for now; reveal in a later release.</summary>
    public const bool CardDuel = false;
}
